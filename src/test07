# k번째수
def solution(array, commands):
    answer = []
    for i in commands:
        a, b, c = i
        x= array[a-1:b]
        x.sort()
        answer.append(x[c-1])

            
    return answer
